name: ins-intent
channels: [conda-forge, defaults]
dependencies:
  - python=3.10
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - transformers>=4.41
      - datasets
      - accelerate
      - torch
      - peft
      - fastapi
      - uvicorn
