name: ins-embed
channels: [conda-forge, defaults]
dependencies:
  - python=3.10
  - faiss-cpu
  - pip
  - pip:
      - sentence-transformers==3.*
      - rank-bm25==0.2.*
      - pandas>=2.1,<2.3
      - numpy>=1.26,<2.0
      - scikit-learn>=1.4,<1.6
      - tqdm
      - matplotlib
      - PyMuPDF
      - unstructured
      - rapidfuzz
      - openai
